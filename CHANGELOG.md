# Changelog

## 0.1.0

- Initialized `STViewer` project skeleton in a standalone directory
- Separated source code into `src/stviewer/`
- Added package metadata with `pyproject.toml`
- Added launcher script in `scripts/`
- Added project documentation scaffold
- Added example CSV data for demo and testing
